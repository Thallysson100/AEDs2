{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug C File",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": ["3","4","5"],               // Aqui você pode adicionar os argumentos para a função main
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "preLaunchTask": "build",
            "miDebuggerPath": "/usr/bin/gdb" // Caminho para o GDB no Linux; ajuste conforme necessário
        }
    ]
}